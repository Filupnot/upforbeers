// Public values, safe to commit and safe to ship in the deployed JS.
// Fill these in after you create the Lambda Function URL and the VAPID keypair (see SETUP.md).
window.UB_CONFIG = {
  FUNCTION_URL: 'https://REPLACE_WITH_FUNCTION_URL.lambda-url.us-west-2.on.aws',
  VAPID_PUBLIC_KEY: 'REPLACE_WITH_VAPID_PUBLIC_KEY',
};
